spring:
  devtools:
    restart:
      enabled: true
  datasource:
    username: root
    password: iruby234
    url: jdbc:mysql://121.4.73.4:3307/wshop
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:

  configuration:
    mapUnderscoreToCamelCase: true
  #    对所有的 resultMap 都进行自动映射
  #    autoMappingBehavior: FULL
  global-config:
    db-config:
      id-type: auto